import { StyleSheet } from "react-native";
import { Metrics } from "../../../theme";

export default StyleSheet.create({
  currentLocation: {
    position: "absolute",
    right: Metrics.smallMargin * 1.5,
    bottom: Metrics.smallMargin * 1.5
  }
});

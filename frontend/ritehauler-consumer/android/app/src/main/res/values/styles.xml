<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimaryDark">@color/statusbar</item>
    </style>

    <style name="SplashTheme" parent="Theme.ReactNative.AppCompat.Light.NoActionBar.FullScreen">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimaryDark">@color/statusbar</item>
    </style>


    <style name="facebookTheme" parent="com_facebook_activity_theme">
        <item name="colorAccent">@color/accent</item>
        <item name="colorPrimaryDark">@color/statusbar</item> <!-- statusbar color -->
    </style>

</resources>
